#include "main.h"
/**
   * mul - multiplies two integers
   * @a: the first integer to multiply
   * main - funtion principal
   * @b: the second integer to multiply
   * Return: the result
   */
int main(void)

{
	int a;
	int b;

	int mul = a * b;

	return (mul);
}
