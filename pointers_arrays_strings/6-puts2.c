#include "main.h"
/**
  * puts2 - prints one character out of two, starting with the first
  *
  * @str: string to look at
  *
  * Return: 0
  */
void puts2(char *str)
{
	int i;
	int len;

	i = 0;
	len = _strlen(str) - 1;

	while (i <= len)
	{
		putchar(str[i]);
		i += 2;
	}
	putchar('\n');

	while (s[] != '\0')
		i++;

	return (i);
}
