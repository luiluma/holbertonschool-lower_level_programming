#include "main.h"
#include <string.h>

/**
  *  _strlen - strlen
  * @s: the string  whose length to return
  * RETURN: the length of a string
  */

int _strlen(char *s)
{

	int a;

	a = strlen(s);
	return (a);
}
