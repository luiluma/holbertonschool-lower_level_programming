#include "main.h"
#include <stdio.h>
/**
   *_abs - calcula el valor absoluto de un entero
   *
   *Description: encontrar el valor absoluto del entero
   *
   *@a: the integer
   *
   *Retorno: 0
   */
int _abs(int a)
{
	if (a < 0)

	{
	a = a - 1;

		return (a);
	}
	else

		return (a);
}

