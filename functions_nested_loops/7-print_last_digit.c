#include "main.h"

/**
   * print_last_digit - prints the last digit of a number
   *
   * @l: the digit we need to find the last number of
   *
   * Return: value of last digit
   */
int print_last_digit(int n)
{

	int l;
	
	if (n < 0)
	{

	l = l * -1;

	_putchar(l + '0');

return (l);	

	}

	else
	{

	_putchar(l + '0');

return (l);

	}

}
