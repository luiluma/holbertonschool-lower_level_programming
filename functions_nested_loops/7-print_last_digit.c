#include "main.h"

/**
   *print_last_digit - last digit of a number
   *
   *Description: prints the last digir of number and variable l and n
   *
   *@n: value
   *
   *Return: always 0
   */
int print_last_digit(int n)
{
	int l;

	if (n < 0)
	{

	l = l * -1;

	_putchar(l + '0');

return (l);

	}

	else
	{

	_putchar(l + '0');

return (l);

	}
}
