#include <stdio.h>
/**
 * main - the numbers of base 16 in lowercase
 *
 *Return: 0
 */
int main(void)
{
	int n = 0;
	char letter = 'a';

	for (n = 0; n < 10; n++)
		putchar (n);

	for (letter = 'a'; letter <= 'f'; leteer++)
		putchar (letter);

return (0);
}
